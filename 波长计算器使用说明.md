# 迈克尔孙干涉仪波长计算器

双击同目录的 `迈克尔孙波长计算器.html`，用 Edge、Chrome 等浏览器打开即可。不用安装软件，也不需要联网。

1. 在 k 框输入累计条纹计数，例如 `0, 50, 100, 150, 200, 250, 300, 350, 400, 450`。
2. 在 d 框输入对应的原始位置读数，单位为 mm。两栏数量相同，至少 4 项且为偶数；k 等间隔递增，d 单调增加或减小。
3. 按老师或实验指导书填写有效误差限 Δ。暂时不知道可以留空，程序仍能计算波长和 A 类不确定度。
4. 点击“计算”。可选填参考波长以显示绝对误差和相对误差。
5. 点击“导出计算报告 TXT”，保存输入、结果、逐差明细和公式说明。关闭页面不会自动保存输入。

“加载示例”使用模拟读数，Δ = 0.0001 mm 仅作演示，不代表已经确认了你所用仪器的误差限。

公式依据提供课件第 15、27、28 页。按前后半组配对，归一化为单个计数间隔的位移，再计算 A 类、B 类、合成与波长标准不确定度。页面下方“公式、参数含义与适用条件”给出完整说明。

误差限 Δ 不等同于仪器分度值，也不一定等同于单个原始读数误差限；它用于课件的 uB(Δd) = Δ / √3，需对应归一化单间隔位移。若老师另有约定，应先确认该参数口径。输出的 ± 是标准不确定度，覆盖因子为 1，不是 95% 置信区间。

附 `检查波长计算器.cjs`，供有 Node.js 的使用者运行数值和交互逻辑检查；日常使用只需要 HTML 文件。
